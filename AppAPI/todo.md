Todo:
-Access control with .NET Core's policy-based authorization
   -Users can look at images and some users could post images (Entity Framework Core for data-access)
-Validation?





When moving to a server:
-nginx as reverse proxy
	-certbot for renewing let'sencrypt certificate automatically.